<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dyslexia.art">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />
    <uses-permission android:name="android.permission.MANAGE_ACCOUNTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Introduction" />
        <activity android:name=".MyCalendar" />
        <activity android:name=".GoogleCalendar" />
        <activity
            android:name=".CalendarPerson"
            android:label="@string/title_activity_calendar_person"
            android:theme="@style/AppTheme" />
        <activity android:name=".PersonalCalendar" />
        <activity android:name=".fileupdate" />
        <activity android:name=".paper" />
        <activity android:name=".meetingpaper" />
        <activity android:name=".exhibition" />
        <activity android:name=".journal" />
        <activity android:name=".book" />
        <activity android:name=".service"></activity>
    </application>

</manifest>